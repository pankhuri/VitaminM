<%= form_for(@bill_type) do |f| %>
  <% if @bill_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bill_type.errors.count, "error") %> prohibited this bill_type from being saved:</h2>

      <ul>
      <% @bill_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
